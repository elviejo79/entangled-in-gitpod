image:
  file: .gitpod.Dockerfile
tasks:
  - init: |
      cabal update
      stack build
    command: |
      python3 -m http.server 8000
      make watch

vscode:
  extensions:
    - haskell.haskell
    - dhall.dhall-lang
